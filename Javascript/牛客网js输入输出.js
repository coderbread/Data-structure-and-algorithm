//多组数据， 两行 => 一行
let input = []
while (line = readline()) {
  input.push(line)
  if (input.length === 2) {
    let data1 = input[0]
    let data2 = input[1]
    input.splice(0)
    var res = ''
    print(res)
  }
}

while (line = readline()) {
    print()
}

//换行输入 print(str1 + '\n' + str2)

//表达式求职
//print(eval(readline()))

let input = []
while(line = read_line()){
  input.push(line)
  if(input.lenth === 2){

  }
}